version: '3.8'
services:
  fastapi:
    build:
      context: ..    
      dockerfile: docker/dockerfile
    restart: always
    container_name: fastapi
    ports:
      - "8000:8000"
    volumes:
      - ../app:/code/app

